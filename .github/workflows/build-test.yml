name: 🔨 Build Test
on:
  pull_request:
  workflow_dispatch:


jobs:  
  build:
    name: Test Builds
    runs-on: ubuntu-latest
    steps:
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: 1.24.x

      - name: Check out code
        uses: actions/checkout@v6

      - name: Test
        run: go test ./...

      - name: Build
        run: go run main.go
        working-directory: examples/
